<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Registro de herramienta</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/resources/css/registroInventario.css">
    <%- include('./partials/boostrap.ejs') %> 

</head>


<body>
  
<%- include('./partials/administrador.ejs') %>

<div class="container">
    <div class="row">
      <div class="col-md-12">
        <div class="bg-success p-3 text-center mt-4">
          <h3>REGISTRO DE HERRAMIENTA</h3>
        </div>
        <div class="mt-4">
          <form id="registro-herramienta" action="/registroHerramienta" method="post">
            <div class="form-group row">
              <label for="tipo" class="col-sm-2 col-form-label">Tipo <span class="text-danger">*</span></label>
              <div class="col-sm-4">
                <input type="text" name="tipo" class="form-control" id="tipo" placeholder="Introduca el tipo del material">
              </div>
            </div>
            <div class="form-group row">
              <label for="color" class="col-sm-2 col-form-label">Color <span class="text-danger">*</span></label>
              <div class="col-sm-4">
                <input type="text" name="color" class="form-control" id="color" placeholder="Introduzca el color del material">
              </div>
            </div>
            <div class="form-group row">
              <div class="col-sm-2"></div>
              <div class="col-sm-4">
                <button type="submit" class="btn btn-success">Nuevo registro</button>
              </div>
            </div>
          </form>
        </div> 
      </div>
    </div>
    </div>
    

    <script>
      document.getElementById('registro-herramienta').addEventListener('submit', function (event) {
               event.preventDefault();
               var form = event.target;
   
               if (form.checkValidity()) {
                   Swal.fire({
                       title: '¿Desea registrar esta herramienta??',
                       icon: 'question',
                       showCancelButton: true,
                       confirmButtonText: 'Aceptar',
                       cancelButtonText: 'Cancelar',
                       confirmButtonColor: '#28a745',
                         cancelButtonColor: '#dc3545'
                   }).then(function (result) {
                       if (result.value) {
                           Swal.fire({
                               title: 'Se ha registrado la herramienta de manera exitosa',
                               icon: 'success',
                               confirmButtonText: 'Aceptar',
                               confirmButtonColor: '#28a745'
                           }).then(function () {
                               form.submit();
                           });
                       }
                   });
               } else {
                  //  Swal.fire({
                  //      title: 'Debe llenar todos los campos para registrar a un usuario',
                  //      icon: 'warning',
                  //      confirmButtonText: 'Aceptar'
                  //  });
               }
           });
   </script>

</body>

<%- include('./partials/footer.ejs') %>

</html>

<!--V3-->